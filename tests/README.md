"""
Test Suite — Warehouse Monitoring System
=========================================
Chạy tất cả:    python tests/run_all.py
Chạy từng test: python tests/test_<name>.py

Yêu cầu:
  pip install paho-mqtt requests

Trước khi test phải chạy:
  cd backend && npm run dev
  cd ai-module && python main.py
  python tools/mqtt_sim.py     (cho test MQTT pipeline)
"""
